pipeline{
    agent any
    stages {
        stage('Crear-Directorio') {
            steps{
                ansiblePlaybook credentialsId: 'oracleDB', extras: 'USUARIO,PASSWORD', installation: 'ansible', inventory: '/etc/ansible/hosts', playbook: '/var/lib/jenkins/workspace/prueba-pipeline-file/CrearUsuario.yml'
            }    
            
        }
   }
}
